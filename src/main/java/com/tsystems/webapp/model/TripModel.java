package com.tsystems.webapp.model;

import javax.persistence.Entity;
import java.util.Date;


//@Entity

public class TripModel {
/*
    private int id;

    public void setId(int id) {
        this.id = id;
    }

    public int getId() {

        return id;
    }

    private String stationName;
    private Date departureDate;
    private Date arrivalDate;
    private String arrivalStation;
    private int trainNumber;

    public String getStationName() {
        return stationName;
    }

    public Date getDepartureDate() {
        return departureDate;
    }

    public Date getArrivalDate() {
        return arrivalDate;
    }

    public String getArrivalStation() {
        return arrivalStation;
    }

    public int getTrainNumber() {
        return trainNumber;
    }

    public void setStationName(String stationName) {
        this.stationName = stationName;
    }

    public void setDepartureDate(Date departureDate) {
        this.departureDate = departureDate;
    }

    public void setArrivalDate(Date arrivalDate) {
        this.arrivalDate = arrivalDate;
    }

    public void setArrivalStation(String arrivalStation) {
        this.arrivalStation = arrivalStation;
    }

    public void setTrainNumber(int trainNumber) {
        this.trainNumber = trainNumber;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) return true;
        if (o == null || getClass() != o.getClass()) return false;

        TripModel tripModel = (TripModel) o;

        if (trainNumber != tripModel.trainNumber) return false;
        if (stationName != null ? !stationName.equals(tripModel.stationName) : tripModel.stationName != null)
            return false;
        if (departureDate != null ? !departureDate.equals(tripModel.departureDate) : tripModel.departureDate != null)
            return false;
        if (arrivalDate != null ? !arrivalDate.equals(tripModel.arrivalDate) : tripModel.arrivalDate != null)
            return false;
        return arrivalStation != null ? arrivalStation.equals(tripModel.arrivalStation) : tripModel.arrivalStation == null;

    }

    @Override
    public int hashCode() {
        int result = stationName != null ? stationName.hashCode() : 0;
        result = 31 * result + (departureDate != null ? departureDate.hashCode() : 0);
        result = 31 * result + (arrivalDate != null ? arrivalDate.hashCode() : 0);
        result = 31 * result + (arrivalStation != null ? arrivalStation.hashCode() : 0);
        result = 31 * result + trainNumber;
        return result;
    }

    @Override
    public String toString() {
        return "TripModel{" +
                "stationName='" + stationName + '\'' +
                ", departureDate=" + departureDate +
                ", arrivalDate=" + arrivalDate +
                ", arrivalStation='" + arrivalStation + '\'' +
                ", trainNumber=" + trainNumber +
                '}';
    }
    */
}
